# Daily screener/dashboard pipeline (Part 2). Separate from the backtest deps so
# CI installs only what the scrape needs. Includes the backtest's data libs
# because enrich.py reuses fetch_bars (Alpaca) for the pullback/retrace context.
finvizfinance>=1.3.0
beautifulsoup4>=4.12.0
lxml>=5.0.0
requests>=2.31.0
alpaca-py>=0.30.0
pandas>=2.1.0
numpy>=1.26.0
pyarrow>=15.0.0
python-dotenv>=1.0.0
tqdm>=4.66.0
# Optional: enables the LLM catalyst fallback when ANTHROPIC_API_KEY is set.
# Pipeline runs rules-only if this import or the key is missing.
anthropic>=0.40.0
